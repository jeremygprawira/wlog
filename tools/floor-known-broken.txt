# Modules whose upgraded dependency set is known to fail.
#
# Every line holds one module dir, relative to the repo root. The -libs step skips
# the upgrade for these modules and says so, because the break belongs to a
# dependency and needs a version decision rather than a code fix.
#
./rpc/gqlgen # the upgraded gqlgen adds a context to ExecutableSchema.Complexity; the fake schema follows at the next bump
